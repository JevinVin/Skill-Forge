---
trigger: always_on
---

- All frontend UI must use a modern dark theme as default (dark background, high-contrast text, accent color for CTAs).
- Interface must feel sleek and interactive: smooth transitions/animations, hover states, loading skeletons instead of blank screens.
- Use a component library or utility CSS (Tailwind) for consistency — no inline styling scattered across files.
- Prioritize clean spacing, readable typography, and a cohesive color palette across all pages.